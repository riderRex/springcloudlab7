server:
  port: 8031 # 指定Eureka实例的端口
eureka:
  instance:
    prefer-ip-address: true # 是否显示主机IP
  client:
    service-url:
      defaultZone:
        http://localhost:8761/eureka/  # 指定Eureka服务端地址
spring:
  application:
    name: eurekauserhystrixdashboard #指定应用名称




